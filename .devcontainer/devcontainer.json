{
  "build": { "dockerfile": "Dockerfile" },
  "image": "ghcr.io/rails/devcontainer/images/ruby:3.3.9",
  "postCreateCommand": "sudo apt update && sudo apt install libpq-dev libsqlite3-dev && bundle"
}

